# 2026-09-15

## 骑迹认证功能：真实数据库联调 + 验收（完成）
- 用户核对交接文档后指示"按照文档完成接下来需求"→ 执行两项未完成事项
- 环境：.env 指向阿里云 ECS 121.40.235.227 的 MySQL(3307)/Redis(6379)，端口连通性均 True
- `go test ./...` 全部通过（含 auth 包单元测试）
- **TestInfrastructure 联调通过**（4.1s，QIJI_INTEGRATION_TEST=1 触发）：MySQL 建库建表/账号创建/查找/唯一约束 + Redis 会话 TTL 过期(1.1s)/限流；测试记录自动清理
- **API 端到端验收 12/12 通过**（curl 脚本 C:\Users\14925\AppData\Local\Temp\qiji-verify-auth.ps1）：
  303 跳转 auth.html / 401 拦截 API / 201 注册 / 409 重复注册 / 401 错误密码 / 200 登录+cookie / me 200 / instances 200 / 首页 200 / 登出 200 / 登出后 me+API 均 401
- 静态资源 auth.html/css/js、boot.js 均 200；read_lints 前端文件零错误
- 服务以真实数据库模式运行于 8081（gatsp.exe 后台进程，工作目录 ga-tsp）
- 验收账号 **demodispatch / dispatch123**（id=3）保留在 qiji_dispatch.qiji_users，供演示/浏览器验收
- 待用户确认：①工作台是否统一黄黑风格 ②认证代码是否提交（须逐项暂存，不用 git add .，且不得混入记忆文件/gatsp 二进制/docs 等既有改动）

## 用户确认结果与执行（同日）
- 三项确认：浏览器验收通过 / 工作台统一黄黑风格 / 现在提交认证代码
- **认证 commit `4b816f9`**（feat: 接入账号登录注册与会话管理，19 files +1618/-13）：逐项暂存 .gitignore、.env.example、go.mod/go.sum、main.go、internal/config、internal/auth、web/auth.*、boot.js、index/dispatch/route 四件套；排除 memory、gatsp/gatsp.exe 二进制、docs/（符合交接文档要求）
- **工作台黄黑主题统一完成**（未提交，待浏览器验收后决定）：
  - dispatch.css 23 处色值：--green→--brand #ffc928，新增 --ink #302b19/--link #9a7208；页面底 #fffcf4、主字 #252422；侧栏炭黑 #201d15 + 品牌黄 #ffd233 + 暖灰导航；按钮白底暖字/primary 黄底深字+hover #ffc721；pill/notice/选中行/统计卡/演示条均浅黄系（#fff1c3/#fdf0bb/#fdf7e0/#fdf3cd）；输入框新增黄色 focus（#ffe69d/#e8bd43）；错误统一 #ac3f29
  - route.js Canvas 5 处：底 #fffcf0、网格 #f0e9d4、路线深琥珀 #c99000、节点黑 #252422、取餐点亮黄 #ffd233、标签 #252422
  - 残留检查：旧绿色值全文件 0 匹配；read_lints 零错误；tsp/experiment 原实验页保持原样不动
- 用户自行提交主题修复：`ecfbe7f fix: 修复工作台页面主题色`

## 三层架构重构设计（simple_tuan，待用户审阅 spec）
- 用户要求：按 controller→logic→dao 分层标准重构目录 + 换 gin + gorm
- 用户拍板：①模块名改 **simple_tuan（某团）**（用户不喜欢 gatsp/qiji 名字）②算法包放 pkg/ ③分三阶段各自测试提交
- 设计文档：`docs/superpowers/specs/2026-09-15-simple-tuan-restructure-design.md`
- 关键设计：cmd/simple_tuan/main.go 纯装配；internal/{controller,logic,dao,models,config}；pkg/{tsp,ga,optimization}；logic 定义 UserStore/SessionStore 接口（消费者侧），dao 实现；controller 不碰 pkg（TSP 端点经 logic/tsp.go 薄封装）
- 命名全面换 ST_ 前缀：模块/二进制/品牌（某团 SIMPLE_TUAN）、env 键 ST_*、库 st_dispatch、表 st_users、cookie st_session、Redis key st:*
- 三阶段：①目录分层（保持旧栈）→ ②gin → ③gorm；不变量：API 契约不变（前端零改动）、att48=10628 金标准、认证行为不变、测试全过
- 仓库子目录 ga-tsp/ 默认保持不改名（避免大规模 rename 与部署路径变化）

## 三阶段重构执行完成（2026-09-15，6 个 commit）
- 阶段① 目录分层：`c0f9591` 模块更名 simple_tuan / `26bfedf` 算法包迁移 pkg/ / `f480684` 业务域三层化 models+logic / `272c63c` 认证三层拆分 + api→controller（合并提交）
- 阶段② gin：`5445a3b` controller 全量换 gin.HandlerFunc，NoRoute 静态兜底，严格解码（DisallowUnknownFields+拒多 JSON）与 `{"error": msg}` 契约不变，契约测试原样通过
- 阶段③ gorm：`bb93cf8` dao/mysql_users.go 换 gorm v1.31.2（AutoMigrate + WithContext + TranslateError 判 409/404），远程 ECS 真实库集成联调 11.78s 通过
- 现状：module `simple_tuan`，依赖 gin v1.12.0 / gorm v1.31.2 / driver-mysql v1.6.0 / go-redis v9.22 / bcrypt；目录 internal/{controller,logic,dao,models,config} + pkg/{tsp,ga,optimization}
- 未做（遗留待办）：Task 5 Step1 logic/tsp.go+experiment.go 薄封装（controller 仍直接 import pkg）、Step3 cmd/ 入口化（main.go 仍在 ga-tsp 根目录，二进制仍 gatsp.exe）；Task 6 全部（QIJI_ → ST_ 前缀、品牌骑迹→某团、表 st_users、cookie st_session）
- 计划文档已更新：`docs/superpowers/plans/2026-09-15-phase1-layered-structure.md`（勾选完成项+差异标注+阶段②③完成记录）；未 push

## 本地启动验证（2026-09-15 上午）
- `go -C ga-tsp build -o gatsp.exe .` 编译成功；发现 8081 已被今早 8:22 旧实例占用 → Stop-Process 后重启新编译版
- 关键坑：**日志在 stderr 不在 stdout**（log 包默认）；Start-Process 用 -RedirectStandardOutput/-RedirectStandardError 分开捕获才看到启动日志
- 启动验证全部通过：`/` 303 跳转、`/auth.html` 200、style.css/auth.js 200、`/api/tsp/instances` 未登录返回 `{"error":"登录已失效"}`（认证链路正常）
- 静态路径是 web/ 平铺：页面 `/auth.html` `/tsp.html` `/experiment.html` `/index.html`，CSS/JS 均在根路径（无 /css/ 子目录）
- 当前服务运行中：进程 gatsp.exe，8081 端口，stdout/stderr 重定向到 ga-tsp/run.log 与 run.err.log

## 校园调度（campus）模块 —— 5 个 commit 完成（09:27~09:54）
- `01098fb` `pkg/campus` 仿真路网（西安邮电大学场景，Default() 返回 places/roads/Distances/Path）+ `logic/campus.go` 订单批次生成（6 场景 uniform/clustered/near/far/outlier/same-place，20~50 单，种子可复现）× `/api/campus/{map,batches/generate}`
- `66d4e6a` `logic/plan.go`（363 行）：三模式 business（最大净收益）/knapsack（纯收入 + DP 验证最优）/route（全送达点路线）；GA 位串选单 + 约束修复 + 最近邻/2-opt/GA refine 路线 → `/api/campus/plan`
- `abdb038` gorm 持久化用户批次与方案快照（models/snapshot.go、dao/campus_snapshots.go、controller/snapshots.go、logic/snapshot.go）→ `/api/campus/{batches/save,plans/save,history,history/:id}`，按 owner 隔离
- `87649de` `/api/campus/compare`：三策略（纯背包 GA / 收入重量比贪心 / 业务 GA）× 5 固定种子重复实验 + ≤10 单 Held-Karp 精确对照（logic/campus_analysis.go）；预算上限 100000 次评估
- `737f421` 前端补录：index.html 重写为校园配送决策四区首页（订单池/配送决策/道路路线/对照实验），旧工作台移至 legacy.html，新增 campus.css + browser_preview_test.go（QIJI_BROWSER_PREVIEW=1 的 opt-in fixture）
- 实测：`go test ./...` 全绿；服务 8081 运行中（09:12 启动，PID 32904）
- **缺口**：`web/campus.js`（17KB 主脚本）未提交但已被 HEAD 的 index.html 引用（克隆后 404）；docs/ 3 份文档、记忆文件、gatsp 二进制与 run*.log 均未提交
- 校园调度这轮未产出 spec/plan 文档（直接迭代开发）；QIJI_ 前缀与表名/cookie 命名统一仍未做

## 前端 JSON 报错定位与修复（2026-09-15 11:00）
- 现象：校园首页报 `Unexpected non-whitespace character after JSON at position 4 (line 1 column 5)`
- 根因：**本地运行的 gatsp.exe 是 09:11:23 编译的旧二进制**（campus 源码 09:33~09:38 才写入），`/api/campus/*` 未注册 → 落到 `r.NoRoute(noRouteGuard)` → `http.FileServer` 对缺失路径返回 404 `text/plain` 正文 `404 page not found` → 前端 `response.json()` 把 `404` 当 JSON 数字解析后卡在第 5 列
- 判定手法：`findstr /M /C:"campus/plan" gatsp.exe` 全部 missing + 源码 mtime(09:33~09:38) > exe mtime(09:11:23)（比 curl 打接口更直接，因为 NoRoute 对未登录请求先回 401，看不出路由缺失）
- 修复：重新 `go build -o gatsp.exe .` 并重启（新 PID 31444，11:00:32 启动）；顺手给 `campus.js` 的 `request()` 加非 JSON 响应兜底（提示「接口未返回 JSON，请确认后端已重新编译启动」）
- 端到端验证（verify_campus.ps1，跑完已删除）：map 12 地点/16 道路；batch 20 单 seed=42；business 选 10 单净收益 90.52 元 / 2640 m / 可行 / 14 ms；knapsack 收入 9626 分 = DP 最优 verified=true；route 10 停靠 / 3240 m；saveBatch #1、savePlan #2、history 正常；compare 3 组策略（业务 GA 9051.8 分 > 纯背包/贪心 8961.47 分），10 单精确最优净收益 6617.47 分
- 教训：**改完后端代码必须重新编译 + 重启**，否则前端只能拿到 404 纯文本

## 品牌更名 + 订单卡片化（2026-09-15 11:05）
- **品牌名「骑迹」→「某团」**：index.html / legacy.html / auth.html（title、brand、brand-symbol 汉字、footer）、analysis.js（导出文件名与图注）、export.js（CSV 首行）、main.go:57 启动日志；副标 `Q I J I · DISPATCH`→`SIMPLE_TUAN · DISPATCH`、`WELCOME TO QIJI`→`WELCOME TO SIMPLE_TUAN`
- **有意保留**：`web/dispatch.js` 的 localStorage 键 `qiji.user.*`（改了会丢老用户本地数据）、后端 `QIJI_` 环境变量 / 表名 `qiji_users` / cookie `qiji_session`（属 Task 6 命名统一，需同步改部署配置）
- **订单从表格行改为卡片**：index.html 的 `<table>` 换成 `<div id="campusOrders" class="order-cards">`；campus.js 新增 `MENU`(12 种餐品：珍珠奶茶/香辣鸡腿堡套餐/脆皮炸鸡桶/牛肉拉面…)+FNV-1a `hash`+`menuFor(order)`+`portion(grams)`，卡片 `.order-card` 含餐品图标圆牌、名称、规格、类目标签、#订单号、可编辑字段（编号/送达位置/重量/收入/交付秒）、配送费大字与删除按钮；主题色走 `--tone/--soft` 变量
- 选择器同步改造：`#campusOrders tr` → `#campusOrders .order-card`（invalidate 与 renderResult 两处）；顺带修掉「送达位置 select 未回填 destinationId、永远显示第一个选项」的显示 bug
- 空态：无订单时卡片区显示引导文案（`.empty`）
- **餐品为纯前端派生仿真样例**：只用订单号稳定映射，不写后端、不参与计算；同一订单号永远同一餐品（可复现、便于答辩讲解）
- 验证：`node --check` 语法通过；30 单命中 12 种餐品（分布 4,4,4,4,3,3,2,2,1,1,1,1）；服务重启 PID 47892，日志「某团配送决策系统已启动」
- **编码坑**：PowerShell 5.1 执行无 BOM 的 `.ps1` 会按 GBK 解码中文字面量 → 脚本内中文替换**全部静默失效**（输出乱码即征兆）；解法：中文用 `[char]0xXXXX` 码点构造，或脚本内只写 ASCII

## 订单卡片重做 + 配送箱动画 + 缓存修复（2026-09-15 11:40）
- **用户反馈「依旧是行展示」的根因**：`noRouteGuard` 原本只给 4 个 HTML 白名单设 `Cache-Control: no-store`，`campus.css`/`campus.js` 等静态资源没有任何缓存头 → 浏览器用启发式缓存吃旧副本。修复：`.css`/`.js` 一律 `no-store`；`isPage` 由白名单改为 `p == "/" || (HasSuffix(".html") && p != "/auth.html")`（**必须排除 auth.html，否则未登录会被 303 重定向到自己死循环**）
- **卡片改为点餐卡风格**（一单一张卡）：`.top`（餐品圆牌 `--soft` 底 + 名称 + 规格 + 标签 + 右上价格）+ `.stats`（**标准重量 / 价值收益 / 配送距离** 三栏，虚线分隔）+ `.foot`（送达地点 + 删除）+ `<details class="tune">调整参数`（5 个输入框折叠进去，卡片不再是表单行）
- **单笔配送距离**：`pkg/campus.Map` 新增 `DepotMeters []float64 json:"depotMeters"`，在 `Build()` 里从 `Distances[0]` 填充（Inf → -1 表示不可达，避免 JSON 序列化 +Inf 报错）；前端 `meters()/distanceOf()` 直接查表，`<1000m` 显示 `412 m`、否则 `1.24 km`、不可达显示 `—`
- **配送箱（右下 fixed 挂件）**：`#crate` + `#crateToggle`（CSS 画的箱子 + 可掀开的盖子 `data-state=packing`）+ `#crateBadge` 计数 + `#crateItems` 筹码列表（点击展开/收起）
- **飞入动画**：`packCrate(selected)` 复制卡片为 `.flying` 克隆体 → Web Animations 三段 transform（抛物线偏移 → 0.62 缩放 → 0.06 缩放淡出），**10 并发 / 35ms stagger / 440ms duration**，视口外卡片跳过飞行直接装袋；原卡片加 `.packed`（顶部绿色渐变条）；`prefers-reduced-motion` 全量降级为瞬时
- 与 `invalidate()` 联动：任何输入变化 → `resetCrate()` 清空箱子与装袋态
- 实测（种子 2026 / uniform / 20 单 / business / 6000g / 45min）：depot 距离 `0,240,480,720,300,540,780,1020,600,840,1080,1320` 米；规划选中 11 单、2640 m、净收益 ¥100.76、41 ms
- 坑：PS 5.1 的 `Invoke-WebRequest -Method Head` 会抛 NullReferenceException（IE 引擎）→ 查响应头改用 `curl.exe -s -I`；`(cmd > $null; cmd | ...)` 这种括号内多语句在 PS 里是语法错误

## 提交记录（2026-09-15 12:10）
- 4 个 commit 已入库（分支 feat/qiji-dispatch，**未 push**）：
  - `6ec97a7` `chore: unify branding to simple_tuan and archive design docs` —— main.go 启动日志、auth.html/legacy.html 标题页脚、analysis.js/export.js 导出名与图表水印统一为「某团」；补录 docs/ 三份设计文档 + .codebuddy 记忆；`.gitignore` 增加 `ga-tsp/run*.log`（12 文件 +867）
  - `f4143ca` `feat: render campus orders as meal cards and fly selected ones into a crate` —— campus.js 379 行（点餐卡 + 配送箱飞入动画）、campus.css +64、index.html 10 行（卡片提示 + 配送箱 DOM）、campus.go +13（Map.DepotMeters），共 4 文件 +458
  - `c0b1dd2` `fix: disable caching for static assets and match html pages by suffix` —— middleware.go 6+/1-
  - `74bd591` `chore: refresh build artifacts` —— gatsp / gatsp.exe
- 提交后 `git status` 干净（run.log / run.err.log 已被忽略）
- 体积：`.git` 63.8 MiB（479 个散对象，未打包）；`gatsp.exe` 由 9 MB 涨到 **43 MB**（本次 go build 未加 `-ldflags "-s -w"`），`gatsp` 9.09 MB —— 后续如需瘦身：strip 重建，或 `git rm --cached` 后加入 .gitignore（涉及历史，先问用户）

## 本地启动项目（2026-09-15 13:36）
- 流程：`go build -o gatsp.exe .`（exit 0）→ 停掉占 8081 的旧进程 → `Start-Process gatsp.exe -WorkingDirectory ga-tsp -RedirectStandardOutput run.log -RedirectStandardError run.err.log -WindowStyle Hidden`
- 依赖连通性：远程 `121.40.235.227` 的 MySQL 3307 / Redis 6379 均 True（TCP 8081 不通，服务只在本地监听）
- 端到端冒烟：`/` 未登录 303 → auth.html 200；注册 `localdemo/localdemo123` 201（**uid=13**，写入远程 qiji_dispatch.qiji_users）；登录 200 + cookie；`/api/auth/me` 200；登录后 `/` 200；`/api/campus/map` 200（13908 字节）；`/api/campus/plan`、`/api/dispatch/select` 传空 body 得 400（证明路由已注册，非 NoRoute 404）
- 登录测试用 `curl.exe --data-binary "@%TEMP%\qiji-login.json"` + `-c/-b cookie.txt`（PowerShell 内联 `-d "{...}"` 转义会被破坏，返回 400 请求格式不正确）
- 浏览器预览已打开 <http://localhost:8081/>；当前 PID 66244（13:35:45 启动）

## 合并 feat/qiji-dispatch → master（2026-09-15 13:39）
- master 原为 `b892a1b`（= merge-base，完全落后），feat 领先 26 个提交 → `git merge --ff-only` **快进成功**，master 现为 `d9d98ce`（本地领先 origin/master 26 个提交，**未 push**）
- **坑：gatsp.exe 被运行中的进程占用时，git 无法 unlink**（`error: unable to unlink old 'ga-tsp/gatsp.exe': Invalid argument`，随后 `fatal: Could not reset index file to revision 'HEAD'`，导致 `git stash push` 半成功）→ 合并/切分支前必须先 `Stop-Process` 掉 gatsp 进程
- 操作顺序：停进程 → `git stash push` → `git checkout master` → `git merge --ff-only feat/qiji-dispatch` → `git stash pop`；stash base 与合并后 HEAD 同为 d9d98ce，pop 干净无冲突
- 合并后 master 工作区仍保留未提交改动：`M .codebuddy/memory/2026-09-15.md`、`M ga-tsp/gatsp.exe`（13:35 用最新源码重编译的 43191296 字节版本，含校区模块）、未跟踪 `ga-tsp/gatsp.exe~`（43160576 字节，11:10 旧构建残留，可删）
- 服务已重启：PID **62296**（13:38:42）；验证 `/` 未登录 303、带 cookie 200、`/api/campus/map` 200
- run.log 重定向后为空（Go log 走 stderr，且被缓冲），以接口响应码判断启动结果更可靠

## 打包部署到阿里云 ECS（2026-09-15 13:45）
- 目标：把 master（d9d98ce，含认证 + 校园调度 + 西校区路网）替换掉 ECS 上 09-14 的旧容器
- **交叉编译必须加 `-ldflags "-s -w"`**：不加产出的 ELF 是 **43 MB**（本地 gatsp.exe 同样 43MB 是同因），加 strip 后 **30 MB**；首次误传 43MB 版，重编译后覆盖
- 命令：`GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -ldflags '-s -w' -o gatsp .`（写成临时 `.ps1` 执行，避开 `cmd /c set` 的引号与变量转义；脚本纯 ASCII）
- 传输：`tar -czf ... --exclude=web/export.test.mjs gatsp web` → `scp` 到 `/opt/ga-tsp/` → 服务器 `rm -rf web gatsp && tar -xzf`（服务器原 web 目录权限 700、只有 6 个 09-14 旧文件）
- **关键改动：容器网络**。mysql/redis 在 `workspace_default`（172.18.0.0/16，网关 172.18.0.1），旧 ga-tsp 在默认 bridge（172.17.0.0/16）且只注入 PORT —— 新版必须改：`--network workspace_default` + `-e QIJI_MYSQL_ADDR=mysql:3306 -e QIJI_REDIS_ADDR=redis:6379`（容器名直连）+ `QIJI_MYSQL_USER=root / QIJI_MYSQL_PASSWORD=sta_go / QIJI_MYSQL_DATABASE=qiji_dispatch / QIJI_CREATE_DATABASE=true / QIJI_COOKIE_SECURE=false`
- 部署后镜像 **52.9 MB**，容器 Up，`--restart unless-stopped`；磁盘 14G/40G（36%）
- **公网验证全通过**（`verify-prod.ps1`，公网 http://121.40.235.227:8001）：未登录 `/` 303、auth.html 200、campus.js/campus-map.js 200、未登录 API 401；login localdemo 200 → 批次 `xupt-changan-west-v2` 20 单 seed=42 → 规划 selected=10 / stops=7 / 2303.82 m / 净收益 8368.66 分 / feasible / 58.5 ms / 曲线 61 点 → `batches/save` 返回 id=3 → `history?kind=batch` 1 条（写读 MySQL 均正常）
- 坑：`/api/campus/history` 的参数名是 **`kind`**（不是 `type`），取值 batch/plan；`/api/campus/batches/generate` 的 count 须在 **20～50**
- 安全组：**8001/TCP 已放行**，本地 curl 公网直接 303
- 本地工作区遗留：`M ga-tsp/gatsp`（新 30MB linux 版，覆盖了仓库里 9MB 旧版）、`M ga-tsp/gatsp.exe`、两个 memory 文件、未跟踪 `ga-tsp/gatsp.exe~`（43MB 可删）

