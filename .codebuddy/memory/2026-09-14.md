# 2026-09-14

## 课程设计项目启动：题目十二 迷宫求解（Go + Web 前后端分离）
- 用户要求：Go 语言实现指导书题目十二（走迷宫），Web 前后端分离模式
- 题目要求：二维数组迷宫 1=墙 0=路，DFS/BFS 求左上→右下最短通路；系统能初始化迷宫、展示探索过程和最终最短路径；需完成课设报告
- 指导书文本已提取到 `c:\Users\14925\Desktop\code\go\courseDesign\.docx_text.txt`（题目十二在 155-162 行附近）
- 设计方案（已向用户给出，待确认后实现）：
  - 后端：Go 标准库 net/http，REST API（POST /api/maze/generate、POST /api/solve、GET /api/preset），零第三方依赖
  - 前端：原生 HTML/CSS/JS + Canvas，放 web/ 目录
  - 算法：BFS（保证最短路）+ DFS（对比），后端生成逐步探索快照 steps，前端动画回放
  - 结构：main.go + internal/maze + internal/solver + web/

## 开发完成并本地运行成功
- 按 TDD 完成：maze（校验/随机生成/预置5x5）、solver（BFS最短路+DFS对比，逐步探索快照）、api（httptest 覆盖）、main.go（:8080，托管 web/ 静态页）
- 前端 web/：index.html + style.css + app.js，Canvas 渲染，点击编辑墙/路、随机生成（密度可调）、题目示例、BFS/DFS 选择、探索动画回放（播放/单步/调速/进度条）、双算法对比统计表与结论
- 全部 `go test ./...` 通过；题目示例迷宫 BFS 最短路径 8 步（9 格）、15 格访问
- 服务本地运行：`go build -o mazeweb.exe .` 后 Start-Process 启动，http://localhost:8080
- 注意：execute_command 直传 PowerShell 命令时 `$` 变量会被吃掉，需写 .ps1 脚本或用 curl.exe -d @file；JSON 传参勿内联转义
