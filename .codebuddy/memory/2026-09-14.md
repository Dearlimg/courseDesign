# 2026-09-14

## 课程设计项目启动：题目十二 迷宫求解（Go + Web 前后端分离）
- 用户要求：Go 语言实现指导书题目十二（走迷宫），Web 前后端分离模式
- 题目要求：二维数组迷宫 1=墙 0=路，DFS/BFS 求左上→右下最短通路；系统能初始化迷宫、展示探索过程和最终最短路径；需完成课设报告
- 指导书文本已提取到 `c:\Users\14925\Desktop\code\go\courseDesign\.docx_text.txt`（题目十二在 155-162 行附近）
- 设计方案（已向用户给出，待确认后实现）：
  - 后端：Go 标准库 net/http，REST API（POST /api/maze/generate、POST /api/solve、GET /api/preset），零第三方依赖
  - 前端：原生 HTML/CSS/JS + Canvas，放 web/ 目录
  - 算法：BFS（保证最短路）+ DFS（对比），后端生成逐步探索快照 steps，前端动画回放
  - 结构：main.go + internal/maze + internal/solver + web/

## 开发完成并本地运行成功
- 按 TDD 完成：maze（校验/随机生成/预置5x5）、solver（BFS最短路+DFS对比，逐步探索快照）、api（httptest 覆盖）、main.go（:8080，托管 web/ 静态页）
- 前端 web/：index.html + style.css + app.js，Canvas 渲染，点击编辑墙/路、随机生成（密度可调）、题目示例、BFS/DFS 选择、探索动画回放（播放/单步/调速/进度条）、双算法对比统计表与结论
- 全部 `go test ./...` 通过；题目示例迷宫 BFS 最短路径 8 步（9 格）、15 格访问
- 服务本地运行：`go build -o mazeweb.exe .` 后 Start-Process 启动，http://localhost:8080
- 注意：execute_command 直传 PowerShell 命令时 `$` 变量会被吃掉，需写 .ps1 脚本或用 curl.exe -d @file；JSON 传参勿内联转义

## 项目结构调整：迷宫项目降级为保底
- 用户要求：题目12 移到独立文件夹作保底，主项目改为难度更高的 **GA 遗传算法 + 可视化**（仍 Go + 前端）
- 已执行：`main.go`/`go.mod`/`internal/`/`web/`/`mazeweb.exe` 全部迁入 `courseDesign/maze-bfs-dfs/`，迁移后编译+测试通过（模块名 mazeweb，端口 8080）

## GA-TSP 主项目开发完成（TDD）
- 位置 `courseDesign/ga-tsp/`，模块名 `gatsp`，端口 **8081**，零第三方依赖
- 结构：main.go + internal/tsp（att48 附件1数据/ATT伪欧氏距离/随机实例）+ internal/ga（引擎+算子）+ internal/api（REST）+ web/（原生 Canvas 前端）
- GA 设计：排列编码、精英保留、可切换 选择(tournament/roulette)×交叉(OX/PMX)×变异(inversion/swap/insert)、每代快照(gen/best/avg/worst/diversity/bestTour)、多样性=旋转归一化独特个体占比
- 亮点：可选 **2-opt 局部搜索（模因算法）**，每代对本代最优个体做 2-opt 并写回种群（拉马克式）
- 关键测试锚点：att48 附件1最优回路长度 == **10628**（ATT 距离金标准，坐标录入已验证正确）
- 实测（att48，种子7）：纯GA 200×500 → 11207（Gap 5.45%，62ms）；GA+2opt → 10996（3.46%）；GA+2opt 400×1000 → 10853（2.12%，241ms）
- 前端：地图路线逐代演化回放（播放/单步/复位/进度条/调速）、收敛曲线(best/avg/已知最优虚线与Gap)、多样性曲线、结果统计表、参数对比实验（同种子仅变一个参数，最多8组，叠加曲线+对比表）
- API：GET /api/instances、GET /api/instances/{name}、POST /api/instance/random、POST /api/solve、POST /api/scan
- 全部 `go test ./...` 通过（tsp/ga/api 三包）；服务已启动：`go build -o gatsp.exe .` + Start-Process（工作目录必须是 ga-tsp 才能托管 web/）
- 待办：课设报告、答辩材料（保底迷宫项目已可交付）

## 前端可视化增强（让用户看到进化/优化过程）
- 后端：ga.Generation 新增 `SampleTours [][]int`（最优 + 均匀采样 3 个，不消耗随机数流，保持确定性，不影响现有测试），用于前端种群云
- 前端地图叠加：
  - 种群云（淡色多路径）—— 直观看到种群从发散到收敛
  - 进化差异高亮：上一代→当代最优回路，新增边绿色发光、消失边红色虚线
  - 找到新最优时闪光脉冲 +「新最优 X」徽标
- HUD 大数据卡片：代次/当代最优/平均/多样性/Gap/较初始改善
- 收敛/多样性曲线：全程淡色轨迹（背景走势）+ 高亮已走部分 + 播放头竖线
- 键盘快捷键：空格播放/暂停、←→单步、Home/End 首末、C 跳收敛点；新增「跳收敛点/末代」按钮
- 默认变异率调到 0.05（扫描数据显示 0.05~0.2 更优）
- 求解中状态条带旋转加载动画
- 验证：`sampleTours=4`/代、静态资源 200、`go test ./...` 28 用例全过
