# 2026-09-16

## 一次性提交「路线进化回放」全部改动（commit f668a4a）
- 未提交内容盘点（10 改动 + 4 新增）：
  - 后端：`internal/logic/plan.go`（planner 增 `capture`/`frames`，refine 回放时关闭局部搜索、用请求 GA 参数）、新增 `internal/logic/route_frames.go`（recordRoute 逐代记录真实历史最佳，含路网几何）与 `route_frames_test.go`、`internal/models/plan.go`（新增 `RouteFrame` + `PlanResult.RouteFrames`）
  - 前端：新增 `web/route-replay.js`（4KB 播放器：播放/单步/从头/终态/调速/进度条/双曲线）、`web/campus.js` 集成（规划前暂停、输入变化清空）、`web/index.html` 新增「路线进化回放」区块、`web/campus.css` 样式
  - 其它：`work/report_measurements/main.go`（课程报告测量脚本）、重新构建的 `gatsp`/`gatsp.exe`、`.codebuddy/memory/*`
- 提交前验证：`go build ./...` 通过；`go test ./internal/logic/ -run RouteReplay -count=1` → **ok 3.184s**
- 垃圾文件处理：`ga-tsp/gatsp.exe~`（编辑器备份）加入 `.gitignore`（新增 `*~` 规则），未入库
- `git add -A` + 单 commit → **`f668a4a`**，14 files changed，+391/-10；提交后工作区干净，本地领先 origin/master **27 个提交（未 push）**
- 未做：重新部署 ECS 8001（容器仍是 d9d98ce 版本，不含本次回放功能）

## 编写根目录项目 README.md（245 行）
- 交付：`c:\Users\14925\Desktop\code\go\courseDesign\README.md`（约 12.5 KB），六节结构：一 主项目某团·校园配送决策（订单池/三模式决策/地图路线/进化回放/对照实验/原工作台 + 技术栈 + 目录 + 快速开始 + API 表 + 算法 + 测试 + 报告脚本）、二 迷宫求解题目十二、三 Docker 部署、四 工作区结构、五 文档索引、六 说明与边界
- 内容经源码核对：23 配送入口/81 道路节点（pkg/campus/README.md）、六种订单场景（logic/campus.go）、注册限流 5 次/分与其它 15 次/分（controller/auth.go limited）、用户名 `^[a-z0-9_]{3,32}$` + 密码 8～72 字节、cookie 名 `qiji_session`、全部 API 路径（controller/*.go）、代码围栏 20 个闭合、所有引用路径存在
- 未写入 ECS 公网 IP（避免暴露服务器地址，等用户决定）；`ga-tsp/README.md` 仍是"骑迹"旧品牌口径，若需同步更新待用户确认
